"use client";

/**
 * Placeholder for the Audio Studio page.
 * The actual LMMS iframe is managed by LmmsPersistentIframe in the layout,
 * which preserves state across route changes using off-screen positioning.
 */
export function AudioStudioEmbed() {
  return <div className="h-full bg-[#1a1a2e]" />;
}
