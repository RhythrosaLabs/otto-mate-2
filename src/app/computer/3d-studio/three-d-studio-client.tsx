"use client";

/**
 * Placeholder for the 3D Studio page.
 * The actual Blockbench iframe is managed by BlockbenchPersistentIframe in the layout,
 * which preserves state across route changes using off-screen positioning.
 */
export function ThreeDStudioEmbed() {
  return <div className="h-full bg-[#2b2b2b]" />;
}
