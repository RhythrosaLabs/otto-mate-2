"use client";

/**
 * Placeholder for the Coding Companion page.
 * The actual Kilocode iframe is managed by KilocodePersistentIframe in the layout,
 * which preserves state across route changes using off-screen positioning.
 */
export function CodingCompanionEmbed() {
  return <div className="h-full bg-[#0a0a0a]" />;
}
