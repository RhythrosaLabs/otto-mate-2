"use client";

/**
 * Placeholder for the App Builder page.
 * The actual Forge iframe is managed by BoltPersistentIframe in the layout,
 * which preserves WebContainers state across route changes using off-screen
 * positioning instead of display:none.
 */
export function AppBuilderEmbed() {
  return <div className="h-full bg-[#0a0a0a]" />;
}
